# Path to your Google Cloud service account key file *inside the container*
# This environment variable is used directly by the google-cloud-vision library.
# The compose.yaml file mounts the local 'google-vision-key.json' to this path.
GOOGLE_APPLICATION_CREDENTIALS=/app/google-vision-key.json

# Optional: Set the port for the service (defaults to 8810 in config.py)
# PORT=8810